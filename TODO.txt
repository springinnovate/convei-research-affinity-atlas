TODO:
* convei searcher
    x the active crawls seems laggy might be duplicated code in index.html, i'll have to check later
    x display not only the active crawls but phases like fetched, analyzed, complete
    * actually report about that person you just selected
        * right now it just shows it's selected
    * collapse the crawled pages
    x the crawling button keeps spinning even after it's done
    x put a count of how many pages are crawled
    x seems like when we ask for a person it will hang while it's also parsing a webpage
    * make a search function
        * most like "x"
        * "search term"
    * search people like pcld
        * people should have references to where they were seen and should consolidate to just one name
            * those can accordion open
    * "search box" to ask questions about people
        * this should trigger bios to be generated if they are "dirty"
    * time to make a permanent database?
    * Rather than hard-code the AI prompts for analysis of the webpage and people, allow that to be a setting, that way we can change it to 'entities'
